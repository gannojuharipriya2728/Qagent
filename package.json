{
  "name": "qagent",
  "version": "1.0.0",
  "private": true,
  "description": "Repository-root helpers. The React client is a self-contained npm project in frontend/ with its own lockfile; the FastAPI service is deployed separately to Render.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "install:frontend": "npm --prefix frontend ci"
  }
}
