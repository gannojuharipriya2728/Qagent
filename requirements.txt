# QAgent backend runtime dependencies.
#
# Upper bounds are deliberate: Render resolves this file on every deploy, so an
# unbounded range lets an upstream major release break production without any
# change on our side.

# --- Web framework -----------------------------------------------------------
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.34.0,<1.0.0
python-multipart>=0.0.20,<0.1.0

# --- Settings & validation ---------------------------------------------------
pydantic[email]>=2.10.0,<3.0.0
pydantic-settings>=2.7.0,<3.0.0
email-validator>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# --- Database ----------------------------------------------------------------
# The asyncio extra pulls in greenlet, which SQLAlchemy's async engine requires.
sqlalchemy[asyncio]>=2.0.0,<3.0.0
asyncpg>=0.30.0,<1.0.0
aiosqlite>=0.20.0,<1.0.0
alembic>=1.14.0,<2.0.0

# --- Security ----------------------------------------------------------------
pyjwt>=2.8.0,<3.0.0
cryptography>=42.0.0,<48.0.0

# --- Document processing & RAG ----------------------------------------------
pypdf>=5.0.0,<7.0.0
python-docx>=1.1.0,<2.0.0
numpy>=1.26.0,<3.0.0
reportlab>=4.2.0,<5.0.0

# --- Outbound HTTP (OpenRouter / NVIDIA NIM) ---------------------------------
httpx>=0.28.0,<1.0.0

# --- Optional S3-compatible object storage (STORAGE_PROVIDER=s3) -------------
boto3>=1.35.0,<2.0.0
